{
  "name": "codex-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A relay service for chunked request forwarding",
  "scripts": {
    "start": "node src/server.js",
    "start:relay": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
